# Claude Code プロジェクト設定

このファイルは Claude Code がプロジェクトで作業する際の指示を定義します。

## コミットメッセージ

- `Co-Authored-By:` 行を含めない
- 日本語でコミットメッセージを書いても良い

## コーディング規約

### Terraform

- `variables.tf` + `var.` より `locals {}` を優先
- `for_each` パターンは避け、明示的なリソース定義を優先
- モジュール分割は必要最小限に

## ドキュメント

- ドキュメントは日本語で作成
- 図解を積極的に使用（ASCII アート）
